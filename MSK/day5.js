// Multiplication Table Program

// Number for the table
let number = 7;

// Loop from 1 to 10
for (let i = 1; i <= 10; i++) {

    // Multiply number with i
    let result = number * i;

    // Print the result
    console.log(number + " x " + i + " = " + result);
}